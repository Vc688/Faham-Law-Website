---
import Base from '../layouts/Base.astro';
import CtaBand from '../components/CtaBand.astro';
import { firm, about, settings, emph } from '../data/site';
import { davidPerson, breadcrumbs } from '../lib/schema';
---
<Base
  title={about.seoTitle}
  description={about.seoDescription}
  jsonld={[davidPerson, breadcrumbs([{ name: 'Home', path: '/' }, { name: 'About', path: '/about' }])]}
>
  <section class="hero about-hero">
    <div class="container">
      <nav class="breadcrumbs" aria-label="Breadcrumb"><a href="/">Home</a><span aria-hidden="true">/</span><span aria-current="page">About</span></nav>
      <div class="ah-grid">
        <div class="ah-copy">
          <p class="eyebrow">{about.eyebrow}</p>
          <h1 class="display" set:html={emph(about.title)} />
          <p class="lead">{about.lead}</p>
          <div class="ah-links">
            <a class="btn" href="/contact">Book a consultation <span class="arrow" aria-hidden="true">→</span></a>
            <a class="btn btn-ghost" href={firm.linkedinDavid} target="_blank" rel="noopener">LinkedIn profile</a>
          </div>
        </div>
        <div class="ah-photo">
          <img src={settings.headshot} alt={settings.headshotAlt} width="1000" height="1500" fetchpriority="high" />
          <p class="ah-cap"><span class="serif">{firm.tagline}</span></p>
        </div>
      </div>
    </div>
  </section>

  <section class="section-tight">
    <div class="container bio-grid">
      <div class="bio prose">
        {about.bio.map((p) => <p>{p}</p>)}
        {about.personal && <blockquote>{about.personal}</blockquote>}
      </div>
      <aside class="facts-card">
        {about.education.length > 0 && (
          <div>
            <h2 class="fc-title">Education</h2>
            <ul role="list">
              {about.education.map((e) => <li><strong>{e.school}</strong><span>{e.degree}</span></li>)}
            </ul>
          </div>
        )}
        {about.barAdmissions.length > 0 && (
          <div>
            <h2 class="fc-title">Bar admissions</h2>
            <ul role="list" class="chips">{about.barAdmissions.map((b) => <li>{b}</li>)}</ul>
          </div>
        )}
        {about.licenses.length > 0 && (
          <div>
            <h2 class="fc-title">Licenses</h2>
            <ul role="list">
              {about.licenses.map((l) => <li><strong>{l.label}</strong><span>{l.value}</span></li>)}
            </ul>
          </div>
        )}
      </aside>
    </div>
  </section>
  <CtaBand />
</Base>
<style>
  .ah-grid { display: grid; grid-template-columns: 1.25fr 1fr; gap: clamp(40px, 6vw, 96px); align-items: center; }
  .ah-copy { display: grid; gap: 24px; }
  .ah-copy .display { font-size: clamp(3rem, 8vw, 7rem); }
  .ah-links { display: flex; flex-wrap: wrap; gap: 12px; }
  .ah-photo { background: var(--surface); border: 1px solid var(--line); border-radius: clamp(24px, 3vw, 36px); padding: 12px; box-shadow: var(--shadow); transform: rotate(1deg); }
  .ah-photo img { width: 100%; aspect-ratio: 2 / 3; object-fit: cover; object-position: 50% 30%; border-radius: calc(clamp(24px, 3vw, 36px) - 10px); }
  .ah-cap { padding: 14px 8px 4px; text-align: center; color: var(--muted); font-style: italic; font-size: 1.1rem; }
  .bio-grid { display: grid; grid-template-columns: 1.5fr 1fr; gap: clamp(40px, 6vw, 96px); align-items: start; padding-top: clamp(32px, 4vw, 56px); border-top: 1px solid var(--line-strong); }
  .bio > p:first-child::first-letter { font-family: var(--serif); float: left; font-size: 5rem; line-height: 0.8; padding: 8px 12px 0 0; color: var(--green-700); font-style: italic; }
  .facts-card { position: sticky; top: calc(var(--header-h) + 24px); background: var(--surface); border: 1px solid var(--line); border-radius: var(--radius); padding: 30px; display: grid; gap: 26px; box-shadow: var(--shadow); }
  .fc-title { font-size: 0.72rem; letter-spacing: 0.18em; text-transform: uppercase; font-weight: 560; color: var(--green-700); font-family: var(--sans); margin-bottom: 12px; }
  .facts-card ul { list-style: none; margin: 0; padding: 0; display: grid; gap: 14px; }
  .facts-card li { display: grid; gap: 2px; }
  .facts-card strong { font-weight: 540; color: var(--green-900); }
  .facts-card span { color: var(--muted); font-size: 0.93rem; }
  .facts-card .chips { display: flex; flex-wrap: wrap; gap: 6px; }
  .facts-card .chips li { padding: 6px 14px; border-radius: 999px; background: var(--mint); color: var(--green-800); font-size: 0.9rem; font-weight: 500; }
  @media (max-width: 900px) {
    .ah-grid, .bio-grid { grid-template-columns: 1fr; }
    .ah-photo { max-width: 420px; transform: none; }
    .facts-card { position: static; }
  }
</style>
